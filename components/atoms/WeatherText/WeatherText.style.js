import { StyleSheet } from "react-native";

export default StyleSheet.create({
  weather: {
    fontSize: 24,
    color: "white",
    textAlign: "center",
    paddingTop: 10,
    fontWeight: "200",
  },
});
